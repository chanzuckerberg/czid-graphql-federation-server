version: "3.8"

services:
  gql:
    build:
      context: .
      dockerfile: Dockerfile
    image: "czid-graphql"
    platform: linux/amd64
    restart: always
    environment:
      API_URL: http://web:3001/graphql
    ports:
      - "4444:4444"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules # Use node_modules built in container, not from the mounted source dir
    networks:
      czidnet:
        aliases:
          - gql.czidnet.localdev
networks:
  czidnet:
    external:
      name: czidnet
